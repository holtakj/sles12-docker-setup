---
- hosts: localhost
  tasks:
    - name: Install Docker CE
      zypper:
        name: docker
        state: present
    - name: Install docker-compose
      zypper:
        name: docker-compose
        state: present