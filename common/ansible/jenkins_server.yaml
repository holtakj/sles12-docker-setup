---
- hosts: localhost
  tasks:
    - name: Create user jenkins_docker
      user:
        name: jenkins_docker
        createhome: no
        shell: /bin/nologin
        system: yes
        state: present
